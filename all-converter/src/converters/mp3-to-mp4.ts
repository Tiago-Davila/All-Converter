import type { Converter } from './types'; import { runMedia } from './media'
import { MP3_SOURCE } from './sources'
export const mp3ToMp4Converter: Converter = { id: 'mp3-to-mp4', label: 'MP3 a MP4', from: [MP3_SOURCE], to: 'mp4', maxSizeMB: 100, limitation: 'La conversión genera un video con waveform automático. Podés usar una portada propia si querés.', async convert(file, progress, options, signal) { return runMedia(file, { operation: 'mp3-mp4', outputName: `${file.name.replace(/\.[^.]+$/, '')}.mp4`, cover: options.cover, coverName: options.coverName, coverMime: options.coverMime, generateWaveform: options.generateWaveform !== false }, progress, signal) } }
