import { loadSheetJs } from '../lib/sheetjs'
import type { Converter } from './types'
export const spreadsheetToPdfConverter: Converter = { id: 'spreadsheet-to-pdf', label: 'Planilla a PDF', from: ['spreadsheet'], to: 'pdf', maxSizeMB: 25, async convert(file, progress, _options, signal) { if (signal.aborted) throw new DOMException('Cancelado', 'AbortError'); const [{ jsPDF }, { default: autoTable }] = await Promise.all([import('jspdf'), import('jspdf-autotable')]); const xlsx = await loadSheetJs(); const workbook = xlsx.read(await file.arrayBuffer(), { type: 'array' }); const rows = xlsx.utils.sheet_to_json<string[]>(workbook.Sheets[workbook.SheetNames[0]], { header: 1 }); const pdf = new jsPDF(); autoTable(pdf, { head: [rows[0] ?? []], body: rows.slice(1) }); const buffer = pdf.output('arraybuffer'); progress({ percent: 100, stage: 'PDF creado' }); return [{ name: file.name.replace(/\.[^.]+$/, '.pdf'), mime: 'application/pdf', buffer, sizeBytes: buffer.byteLength, previewKind: 'pdf' }] } }
