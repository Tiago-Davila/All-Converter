/**
 * ConversionCard (capa UI 002): re-exporta el componente de 001.
 * Migración T042: la UI layer expone este componente desde src/ui/components/,
 * conservando el comportamiento y los tests de 001 sin duplicar código.
 */
export { ConversionCard } from '../../components/ConversionCard'
